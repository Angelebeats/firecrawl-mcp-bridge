--- EXTERNAL BRAIN REASONING ---
Locate the app's LevelDB folder in its Electron user data directory, then use a LevelDB reader or library to open the database and inspect key-value pairs, where tokens are usually stored under specific keys like "authToken" or "token".
